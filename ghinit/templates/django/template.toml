[meta]
name = "Django Project"
description = "Minimal Django project layout with a settings package."
language = "Python"
post_install = "Run: pip install -r requirements.txt && python manage.py migrate"
